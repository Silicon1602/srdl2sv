---
module_declaration: |-
    module {name} (
    // Bus I/O
    // TODO

    // Inputs
    {inputs}

    // Outputs
    {outputs}
    );
input_port: |-
    input  {packed_dim:15s}{name:25s} {unpacked_dim},
output_port: |-
    output {packed_dim:15s}{name:25s} {unpacked_dim},
